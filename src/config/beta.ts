/**
 * Product-scope switches for the closed beta. Chat remains in the codebase as
 * a preview scaffold, but is intentionally not exposed in beta navigation.
 */
export const CHAT_BETA_ENABLED = false;
