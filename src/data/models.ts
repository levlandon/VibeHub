/**
 * @deprecated Mock models data has been replaced by the real data layer
 * in `src/services/models/index.ts` (fetching dynamically from OpenRouter).
 * This file is kept only for backwards compatibility or tests.
 */
import type { Model } from "../types/models";

export const MODELS: Model[] = [];
